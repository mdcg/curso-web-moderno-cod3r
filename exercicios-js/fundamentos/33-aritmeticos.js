const [a, b, c, d] = [3, 5, 1, 15]
const soma = a + b + c + d
const subtracao = a - b
const multiplicao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicao, divisao, modulo)